@model IEnumerable<DoctorDTO>

@{
    ViewData["Title"] = "Edit Doctor";
}

<h1 class="mb-4">@ViewData["Title"]</h1>

<!-- Table hiển thị danh sách các patient -->
<table class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>#</th> <!-- Cột số thứ tự -->
            <th>Patient Name</th>
            <th>Gender</th>
            <th>Age</th>
            <th>Phone Number</th>
            <th>Address</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var (doctor, index) in Model.Select((value, i) => (value, i)))
        {
            <tr>
                <td>@(index + 1)</td> <!-- Số thứ tự -->
                <td>@doctor.FirstName @doctor.LastName</td>
                <td>@(doctor.Gender == 0 ? "Male" : "Female")</td>
                <td>@(DateTime.Now.Year - doctor.DateOfBirth?.Year)</td>
                <td>@doctor.PhoneNumber</td>
                <td>@doctor.Address</td>
                <td>
                    <!-- Nút Edit -->
                    <a href="@Url.Action("Edit", new { id = doctor.DoctorID })" class="btn btn-sm btn-primary">Edit</a>

                    <!-- Nút Delete -->
                    <a href="@Url.Action("Delete", new { id = doctor.DoctorID })" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>


